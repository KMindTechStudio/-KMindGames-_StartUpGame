using UnityEngine;

public static class Helpers
{
    public enum GateType
    {
        In,
        Out,
    }
    public enum GatePosition
    {
        North,
        South,
        East,
        West,
    }
}
